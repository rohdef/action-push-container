(function (_, kotlin_kotlin) {
  'use strict';
  //region block: imports
  var VOID = kotlin_kotlin.$_$.g;
  //endregion
  //region block: pre-declaration
  //endregion
  function copy(_this__u8e3s4, signal, highWaterMark, objectMode, destroy, autoDestroy, construct, emitClose, encoding, read, signal_0, highWaterMark_0, objectMode_0, destroy_0, autoDestroy_0, construct_0, emitClose_0, writev, final, decodeStrings, defaultEncoding, write, writableObjectMode, writableCorked, writev_0, final_0, destroy_1, allowHalfOpen, writableHighWaterMark, write_0, readableHighWaterMark, readableObjectMode, construct_1, read_0) {
    signal = signal === VOID ? VOID : signal;
    highWaterMark = highWaterMark === VOID ? VOID : highWaterMark;
    objectMode = objectMode === VOID ? VOID : objectMode;
    destroy = destroy === VOID ? VOID : destroy;
    autoDestroy = autoDestroy === VOID ? VOID : autoDestroy;
    construct = construct === VOID ? VOID : construct;
    emitClose = emitClose === VOID ? VOID : emitClose;
    encoding = encoding === VOID ? VOID : encoding;
    read = read === VOID ? VOID : read;
    signal_0 = signal_0 === VOID ? VOID : signal_0;
    highWaterMark_0 = highWaterMark_0 === VOID ? VOID : highWaterMark_0;
    objectMode_0 = objectMode_0 === VOID ? VOID : objectMode_0;
    destroy_0 = destroy_0 === VOID ? VOID : destroy_0;
    autoDestroy_0 = autoDestroy_0 === VOID ? VOID : autoDestroy_0;
    construct_0 = construct_0 === VOID ? VOID : construct_0;
    emitClose_0 = emitClose_0 === VOID ? VOID : emitClose_0;
    writev = writev === VOID ? VOID : writev;
    final = final === VOID ? VOID : final;
    decodeStrings = decodeStrings === VOID ? VOID : decodeStrings;
    defaultEncoding = defaultEncoding === VOID ? VOID : defaultEncoding;
    write = write === VOID ? VOID : write;
    writableObjectMode = writableObjectMode === VOID ? VOID : writableObjectMode;
    writableCorked = writableCorked === VOID ? VOID : writableCorked;
    writev_0 = writev_0 === VOID ? VOID : writev_0;
    final_0 = final_0 === VOID ? VOID : final_0;
    destroy_1 = destroy_1 === VOID ? VOID : destroy_1;
    allowHalfOpen = allowHalfOpen === VOID ? VOID : allowHalfOpen;
    writableHighWaterMark = writableHighWaterMark === VOID ? VOID : writableHighWaterMark;
    write_0 = write_0 === VOID ? VOID : write_0;
    readableHighWaterMark = readableHighWaterMark === VOID ? VOID : readableHighWaterMark;
    readableObjectMode = readableObjectMode === VOID ? VOID : readableObjectMode;
    construct_1 = construct_1 === VOID ? VOID : construct_1;
    read_0 = read_0 === VOID ? VOID : read_0;
    return Object.assign({}, _this__u8e3s4, {signal: signal_0, highWaterMark: highWaterMark_0, objectMode: objectMode_0, destroy: destroy_1, autoDestroy: autoDestroy_0, construct: construct_1, emitClose: emitClose_0, encoding: encoding, read: read_0, signal: signal_0, highWaterMark: highWaterMark_0, objectMode: objectMode_0, destroy: destroy_1, autoDestroy: autoDestroy_0, construct: construct_1, emitClose: emitClose_0, writev: writev_0, final: final_0, decodeStrings: decodeStrings, defaultEncoding: defaultEncoding, write: write_0, writableObjectMode: writableObjectMode, writableCorked: writableCorked, writev: writev_0, final: final_0, destroy: destroy_1, allowHalfOpen: allowHalfOpen, writableHighWaterMark: writableHighWaterMark, write: write_0, readableHighWaterMark: readableHighWaterMark, readableObjectMode: readableObjectMode, construct: construct_1, read: read_0});
  }
  function invoke(_this__u8e3s4, signal, highWaterMark, objectMode, destroy, autoDestroy, construct, emitClose, encoding, read, signal_0, highWaterMark_0, objectMode_0, destroy_0, autoDestroy_0, construct_0, emitClose_0, writev, final, decodeStrings, defaultEncoding, write, writableObjectMode, writableCorked, writev_0, final_0, destroy_1, allowHalfOpen, writableHighWaterMark, write_0, readableHighWaterMark, readableObjectMode, construct_1, read_0) {
    signal = signal === VOID ? VOID : signal;
    highWaterMark = highWaterMark === VOID ? VOID : highWaterMark;
    objectMode = objectMode === VOID ? VOID : objectMode;
    destroy = destroy === VOID ? VOID : destroy;
    autoDestroy = autoDestroy === VOID ? VOID : autoDestroy;
    construct = construct === VOID ? VOID : construct;
    emitClose = emitClose === VOID ? VOID : emitClose;
    encoding = encoding === VOID ? VOID : encoding;
    read = read === VOID ? VOID : read;
    signal_0 = signal_0 === VOID ? VOID : signal_0;
    highWaterMark_0 = highWaterMark_0 === VOID ? VOID : highWaterMark_0;
    objectMode_0 = objectMode_0 === VOID ? VOID : objectMode_0;
    destroy_0 = destroy_0 === VOID ? VOID : destroy_0;
    autoDestroy_0 = autoDestroy_0 === VOID ? VOID : autoDestroy_0;
    construct_0 = construct_0 === VOID ? VOID : construct_0;
    emitClose_0 = emitClose_0 === VOID ? VOID : emitClose_0;
    writev = writev === VOID ? VOID : writev;
    final = final === VOID ? VOID : final;
    decodeStrings = decodeStrings === VOID ? VOID : decodeStrings;
    defaultEncoding = defaultEncoding === VOID ? VOID : defaultEncoding;
    write = write === VOID ? VOID : write;
    writableObjectMode = writableObjectMode === VOID ? VOID : writableObjectMode;
    writableCorked = writableCorked === VOID ? VOID : writableCorked;
    writev_0 = writev_0 === VOID ? VOID : writev_0;
    final_0 = final_0 === VOID ? VOID : final_0;
    destroy_1 = destroy_1 === VOID ? VOID : destroy_1;
    allowHalfOpen = allowHalfOpen === VOID ? VOID : allowHalfOpen;
    writableHighWaterMark = writableHighWaterMark === VOID ? VOID : writableHighWaterMark;
    write_0 = write_0 === VOID ? VOID : write_0;
    readableHighWaterMark = readableHighWaterMark === VOID ? VOID : readableHighWaterMark;
    readableObjectMode = readableObjectMode === VOID ? VOID : readableObjectMode;
    construct_1 = construct_1 === VOID ? VOID : construct_1;
    read_0 = read_0 === VOID ? VOID : read_0;
    return {signal: signal_0, highWaterMark: highWaterMark_0, objectMode: objectMode_0, destroy: destroy_1, autoDestroy: autoDestroy_0, construct: construct_1, emitClose: emitClose_0, encoding: encoding, read: read_0, signal: signal_0, highWaterMark: highWaterMark_0, objectMode: objectMode_0, destroy: destroy_1, autoDestroy: autoDestroy_0, construct: construct_1, emitClose: emitClose_0, writev: writev_0, final: final_0, decodeStrings: decodeStrings, defaultEncoding: defaultEncoding, write: write_0, writableObjectMode: writableObjectMode, writableCorked: writableCorked, writev: writev_0, final: final_0, destroy: destroy_1, allowHalfOpen: allowHalfOpen, writableHighWaterMark: writableHighWaterMark, write: write_0, readableHighWaterMark: readableHighWaterMark, readableObjectMode: readableObjectMode, construct: construct_1, read: read_0};
  }
  function copy_0(_this__u8e3s4, signal, highWaterMark, objectMode, destroy, autoDestroy, construct, emitClose, encoding, read) {
    signal = signal === VOID ? VOID : signal;
    highWaterMark = highWaterMark === VOID ? VOID : highWaterMark;
    objectMode = objectMode === VOID ? VOID : objectMode;
    destroy = destroy === VOID ? VOID : destroy;
    autoDestroy = autoDestroy === VOID ? VOID : autoDestroy;
    construct = construct === VOID ? VOID : construct;
    emitClose = emitClose === VOID ? VOID : emitClose;
    encoding = encoding === VOID ? VOID : encoding;
    read = read === VOID ? VOID : read;
    return Object.assign({}, _this__u8e3s4, {signal: signal, highWaterMark: highWaterMark, objectMode: objectMode, destroy: destroy, autoDestroy: autoDestroy, construct: construct, emitClose: emitClose, encoding: encoding, read: read});
  }
  function invoke_0(_this__u8e3s4, signal, highWaterMark, objectMode, destroy, autoDestroy, construct, emitClose, encoding, read) {
    signal = signal === VOID ? VOID : signal;
    highWaterMark = highWaterMark === VOID ? VOID : highWaterMark;
    objectMode = objectMode === VOID ? VOID : objectMode;
    destroy = destroy === VOID ? VOID : destroy;
    autoDestroy = autoDestroy === VOID ? VOID : autoDestroy;
    construct = construct === VOID ? VOID : construct;
    emitClose = emitClose === VOID ? VOID : emitClose;
    encoding = encoding === VOID ? VOID : encoding;
    read = read === VOID ? VOID : read;
    return {signal: signal, highWaterMark: highWaterMark, objectMode: objectMode, destroy: destroy, autoDestroy: autoDestroy, construct: construct, emitClose: emitClose, encoding: encoding, read: read};
  }
  function copy_1(_this__u8e3s4, signal, highWaterMark, objectMode, destroy, autoDestroy, construct, emitClose) {
    signal = signal === VOID ? VOID : signal;
    highWaterMark = highWaterMark === VOID ? VOID : highWaterMark;
    objectMode = objectMode === VOID ? VOID : objectMode;
    destroy = destroy === VOID ? VOID : destroy;
    autoDestroy = autoDestroy === VOID ? VOID : autoDestroy;
    construct = construct === VOID ? VOID : construct;
    emitClose = emitClose === VOID ? VOID : emitClose;
    return Object.assign({}, _this__u8e3s4, {signal: signal, highWaterMark: highWaterMark, objectMode: objectMode, destroy: destroy, autoDestroy: autoDestroy, construct: construct, emitClose: emitClose});
  }
  function invoke_1(_this__u8e3s4, signal, highWaterMark, objectMode, destroy, autoDestroy, construct, emitClose) {
    signal = signal === VOID ? VOID : signal;
    highWaterMark = highWaterMark === VOID ? VOID : highWaterMark;
    objectMode = objectMode === VOID ? VOID : objectMode;
    destroy = destroy === VOID ? VOID : destroy;
    autoDestroy = autoDestroy === VOID ? VOID : autoDestroy;
    construct = construct === VOID ? VOID : construct;
    emitClose = emitClose === VOID ? VOID : emitClose;
    return {signal: signal, highWaterMark: highWaterMark, objectMode: objectMode, destroy: destroy, autoDestroy: autoDestroy, construct: construct, emitClose: emitClose};
  }
  function copy_2(_this__u8e3s4, signal, highWaterMark, objectMode, destroy, autoDestroy, construct, emitClose, writev, final, decodeStrings, defaultEncoding, write) {
    signal = signal === VOID ? VOID : signal;
    highWaterMark = highWaterMark === VOID ? VOID : highWaterMark;
    objectMode = objectMode === VOID ? VOID : objectMode;
    destroy = destroy === VOID ? VOID : destroy;
    autoDestroy = autoDestroy === VOID ? VOID : autoDestroy;
    construct = construct === VOID ? VOID : construct;
    emitClose = emitClose === VOID ? VOID : emitClose;
    writev = writev === VOID ? VOID : writev;
    final = final === VOID ? VOID : final;
    decodeStrings = decodeStrings === VOID ? VOID : decodeStrings;
    defaultEncoding = defaultEncoding === VOID ? VOID : defaultEncoding;
    write = write === VOID ? VOID : write;
    return Object.assign({}, _this__u8e3s4, {signal: signal, highWaterMark: highWaterMark, objectMode: objectMode, destroy: destroy, autoDestroy: autoDestroy, construct: construct, emitClose: emitClose, writev: writev, final: final, decodeStrings: decodeStrings, defaultEncoding: defaultEncoding, write: write});
  }
  function invoke_2(_this__u8e3s4, signal, highWaterMark, objectMode, destroy, autoDestroy, construct, emitClose, writev, final, decodeStrings, defaultEncoding, write) {
    signal = signal === VOID ? VOID : signal;
    highWaterMark = highWaterMark === VOID ? VOID : highWaterMark;
    objectMode = objectMode === VOID ? VOID : objectMode;
    destroy = destroy === VOID ? VOID : destroy;
    autoDestroy = autoDestroy === VOID ? VOID : autoDestroy;
    construct = construct === VOID ? VOID : construct;
    emitClose = emitClose === VOID ? VOID : emitClose;
    writev = writev === VOID ? VOID : writev;
    final = final === VOID ? VOID : final;
    decodeStrings = decodeStrings === VOID ? VOID : decodeStrings;
    defaultEncoding = defaultEncoding === VOID ? VOID : defaultEncoding;
    write = write === VOID ? VOID : write;
    return {signal: signal, highWaterMark: highWaterMark, objectMode: objectMode, destroy: destroy, autoDestroy: autoDestroy, construct: construct, emitClose: emitClose, writev: writev, final: final, decodeStrings: decodeStrings, defaultEncoding: defaultEncoding, write: write};
  }
  return _;
}(module.exports, require('./kotlin-kotlin-stdlib.js')));

//# sourceMappingURL=kotlin-node.js.map
